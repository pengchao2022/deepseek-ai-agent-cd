# install external secrets

# 1. 添加 External Secrets Helm 仓库
helm repo add external-secrets https://charts.external-secrets.io
helm repo update

# 2. 安装 Operator 到 external-secrets 命名空间
helm install external-secrets external-secrets/external-secrets \
  --namespace external-secrets \
  --create-namespace


# delete app in argocd

kubectl patch application deepseek-agent -n argocd --type=json -p='[{"op": "remove", "path": "/metadata/finalizers"}]'


# 1. 切换到你的功能分支
git checkout feature/GTP-54273-deepseek-cd-new

# 2. 拉取远端最新代码
git fetch origin

# 3. 将远端的 main 分支合并到你的当前分支
git merge origin/main


# call the AI agent get EKS node memory and cpu usage
curl -s -X POST "k8s-aiagent-deepseek-cae5d57dc9-574759001.us-east-1.elb.amazonaws.com/chat" \
     -H "Host: agent.awsmpc.asia" \
     -H "Content-Type: application/json" \
     -d '{"prompt": "帮我检查一下当前 EKS 集群各个节点的内存和 CPU 使用情况"}' | jq -r '.response'



# call the AI agent get eks pod logs
curl -s -X POST "k8s-aiagent-deepseek-cae5d57dc9-574759001.us-east-1.elb.amazonaws.com/chat" \
     -H "Host: agent.awsmpc.asia" \
     -H "Content-Type: application/json" \
     -d '{"prompt": "请帮我获取 pod deepseek-agent-6bcd5d59df-5jf7q 的最近 30 行日志"}' | jq -r '.response'


